function gameMenu() {
    const menu = ["1 = Play", "2 = Quit"] 
    for(let i=0; i<menu.length; i++) {
        console.log(menu[i]);
    }
}

function boardScore() {
    const board = ["1", "2", "3", "4", "5", "6", "7", "8", "9"]
}

